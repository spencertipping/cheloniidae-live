// Torus

for (var i = 0; i < 40; ++i) {
  t.pitch(90).jump(100); 
  for (var j = 0; j < 30; ++j) {
    t.move(12).pitch(12);
  } 
  t.jump(-100).pitch(-90).turn(90); 
  t.jump(5).turn(-90).bank(9);
}
